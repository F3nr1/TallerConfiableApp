@model IEnumerable<TallerMecanicoCApp.Models.Soat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaVencimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PuedeTransitar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehiculo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaVencimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PuedeTransitar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vehiculo.VehiculoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SoatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SoatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SoatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
