@model IEnumerable<TallerMecanicoCApp.Models.Mecanico>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NivelEstudios)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NivelEstudios)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.PersonaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MecanicoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MecanicoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MecanicoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
